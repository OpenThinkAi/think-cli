services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "4823"
      THINK_DB_PATH: "/data/open-think.sqlite"
    ports:
      - "4823:4823"
    volumes:
      - open-think-data:/data

volumes:
  open-think-data:
