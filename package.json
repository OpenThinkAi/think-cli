{
  "name": "open-think-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo for the open-think CLI (single workspace; the proxy server folded into the CLI in v0.5)",
  "workspaces": [
    "packages/cli"
  ],
  "scripts": {
    "build": "bun run --cwd packages/cli build",
    "test": "bun run --cwd packages/cli test",
    "dev": "bun run --cwd packages/cli dev"
  },
  "homepage": "https://openthink.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpenThinkAi/think-cli.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "overrides": {
    "onnxruntime-web": "1.26.0"
  }
}
